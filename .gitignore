node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files — never commit secrets
.env
.env.local
.env.*.local
# Replit runtime state
.local/
.config/
.upm/

# Local admin helper for reading OTPs (never commit)
get-otp.ts
